model {

  for (i in 1:N){
    y[i] ~ dbin ( p[i], M)  				#stochastic node 
 	logit(p[i])<- Z[i]						#logical node
   Z[i]~ dnorm(mu[i], tau)			#stochastic node
   mu[i]<- alpha + beta*x[i]		#logical node
  }
#Priors
  alpha ~ dnorm(0, 0.5)
  beta ~ dnorm(0, 0.5)
  tau ~ dgamma(4,3) 

}