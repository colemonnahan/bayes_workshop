model {
  #Priors
  theta~dnorm(1, 1/(.5*.5))
  beta1~dnorm(0,1)
  beta2~dnorm(0,1)
  # likelihood
  for(i in 1:R){
     p[i] <- ilogit(theta + beta1*x1[i] + beta2*x2[i])
     y[i] ~ dbin(p[i], N[i]) 
  }
  # posterior predictive distribution
  for(i in 1:R){
  # generate 1 for each replicated data set
     ypred[i] ~ dbin(p[i],N[i])
  }
}